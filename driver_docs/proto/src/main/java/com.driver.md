## Folders
- **[google](com/google.driver.md)**: The `google` folder in the `gson` codebase contains a `gson` subfolder that provides a type adapter for GSON to handle protocol buffers, facilitating the serialization and deserialization of enums and custom proto field names.

