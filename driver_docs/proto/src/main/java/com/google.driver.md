## Folders
- **[gson](google/gson.driver.md)**: The `gson` folder in the codebase contains a `protobuf` folder that includes a type adapter for GSON to handle protocol buffers, enabling serialization and deserialization of enums and custom proto field names.

