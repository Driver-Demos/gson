## Folders
- **[com](java/com.driver.md)**: The `com` folder in the `gson` codebase contains a `google` subfolder dedicated to JUnit tests for evaluating Gson's serialization and deserialization features, particularly for Java 17 records and classes.

