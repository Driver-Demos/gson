## Folders
- **[META-INF](resources/META-INF.driver.md)**: The `META-INF` folder in the `gson` codebase contains a `native-image` subfolder with configuration files for enabling reflection in GraalVM native images.

