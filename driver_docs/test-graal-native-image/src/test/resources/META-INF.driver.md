## Folders
- **[native-image](META-INF/native-image.driver.md)**: The `native-image` folder in the `gson` codebase contains configuration files for enabling reflection in GraalVM native images, specifically through the `reflect-config.json` file.

