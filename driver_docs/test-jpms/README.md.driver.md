# Purpose
The file describes a Maven module named "test-jpms" that is dedicated to testing the compatibility and functionality of Gson's `module-info.class` with the Java Platform Module System (JPMS). It ensures that the module can be effectively utilized by other projects, focusing on testing the final JAR rather than just the compiled classes.
