## Folders
- **[java](main/java.driver.md)**: The `java` folder in the `gson` codebase contains a `module-info.java` file that defines a dummy module to ensure compatibility with the Maven Compiler Plugin when the module descriptor is present only in test sources.

