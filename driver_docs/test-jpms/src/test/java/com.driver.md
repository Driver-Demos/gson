## Folders
- **[google](com/google.driver.md)**: The `google` folder in the `gson` codebase contains a `gson` subfolder focused on testing the Java Platform Module System's package exports and reflection accessibility.

