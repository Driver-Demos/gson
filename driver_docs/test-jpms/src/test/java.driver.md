## Folders
- **[com](java/com.driver.md)**: The `com` folder in the `gson` codebase contains a `google` subfolder dedicated to testing the Java Platform Module System's package exports and reflection accessibility.

## Files
- **[module-info.java](java/module-info.java.driver.md)**: The `module-info.java` file defines the module `com.google.gson.jpms_test`, specifying its dependencies on `com.google.gson`, `junit`, and `truth`, and configuring package access for testing purposes.
