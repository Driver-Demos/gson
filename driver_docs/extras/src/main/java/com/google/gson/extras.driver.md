## Folders
- **[examples](extras/examples.driver.md)**: The `examples` folder in the `gson` codebase contains a subfolder named `rawcollections`, which provides a Java example for using Gson to handle serialization and deserialization of raw collections with mixed data types.

