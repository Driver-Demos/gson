## Folders
- **[rawcollections](examples/rawcollections.driver.md)**: The `rawcollections` folder in the `gson` codebase contains a Java example file demonstrating the use of Gson for serializing and deserializing raw collections with mixed data types.

