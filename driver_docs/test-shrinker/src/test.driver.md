## Folders
- **[java](test/java.driver.md)**: The `java` folder in the `gson` codebase contains the `com` directory, which houses integration tests for verifying the functionality of shrunken and obfuscated JARs using ProGuard and R8.

