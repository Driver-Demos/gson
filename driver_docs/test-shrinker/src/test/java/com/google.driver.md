## Folders
- **[gson](google/gson.driver.md)**: The `gson` folder in the codebase contains integration tests for verifying the behavior of shrunken and obfuscated JARs using ProGuard and R8, located in the `it` subfolder.

