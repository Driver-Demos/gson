## Folders
- **[google](com/google.driver.md)**: The `google` folder in the `gson` codebase contains a `gson` subfolder that includes internal build configuration files for defining versioning information during the Maven build process.

