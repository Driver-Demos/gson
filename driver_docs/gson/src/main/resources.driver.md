## Folders
- **[META-INF](resources/META-INF.driver.md)**: The `META-INF` folder in the `gson` codebase contains a `proguard` folder with configuration rules for ProGuard and R8 to optimize code related to the Gson library.

